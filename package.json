{
  "name": "tech-adventure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:social-preview": "node scripts/generate-social-preview.mjs",
    "predev": "npm run generate:social-preview",
    "dev": "astro dev",
    "check": "astro check",
    "pretest": "npm run generate:social-preview",
    "test": "node --experimental-strip-types --test",
    "prebuild": "npm run generate:social-preview",
    "build": "astro build",
    "verify": "npm run check && npm test && npm run build"
  },
  "dependencies": {
    "astro": "^7.2.9",
    "devicon": "^2.17.0",
    "simple-icons": "^16.29.0",
    "thesvg": "^3.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "5.9.3",
    "wrangler": "^4.127.1"
  },
  "engines": {
    "node": ">=22"
  }
}
